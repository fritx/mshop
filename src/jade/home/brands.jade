//-
  ul.pure-g
    each brand in brands
      li.pure-u-1-2
        a(href='items.html?brand=#{brand.name}')= brand.name
ul.sort-wrapper.pure-g
  each brand in brands
    li.pure-u-1-3
      a(href='items.html?brand=#{brand.name}')= brand.name
