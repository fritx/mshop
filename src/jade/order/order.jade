form.pure-form
  include profile
  .pure-g.pay-box
    .pure-u-1
      span.trailing 付款方式:
      span.way 货到付款
    .pure-u-1
      .cost-box.pull-right
        span.trailing 合计:
        span.price ￥#{cost.toFixed(1)}
  fieldset.pure-skin-pink.submit-box
    input(type='text', name='message',
    placeholder='给GreatMe留言', value=profile.message||'')
    button.pure-button.pure-button-primary.submit-btn(
    onclick='submitOrder();return false') 提交订单
